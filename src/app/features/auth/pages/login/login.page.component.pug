GridLayout.login-page-wrapper(rows='50, *, auto')
  Label.title(row='0', [text]='"features.auth.pages.auth.login" | translate')
  StackLayout(row='1')
    pt-login-form((loginInitiated)='onLogin($event)')
  StackLayout(row='2')
    Label.smaller(text='Need an account?')
    Button([nsRouterLink]='["/auth/register"]', [text]='"features.auth.pages.auth.register" | translate')
