import { Frame, Page } from '@nativescript/core';

export function setStatusBarColors() {
  // TODO: make it work!
  // Frame.topmost().ios.controller.statusBarStyle = UIStatusBarStyle.LightContent;
  // console.log('statusBarColors iOS!!!!!!!!!!!!!', Frame.topmost().ios.controller.statusBarStyle);
}
